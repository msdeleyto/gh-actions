name: Renovate

on:
  workflow_call:
    secrets:
      RENOVATE_PRIVATE_KEY:
        required: true

permissions: {}

jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - name: Get token
        id: get_token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          private-key: ${{ secrets.RENOVATE_PRIVATE_KEY }}
          client-id: Iv23liMra90GUVamx7UQ
          owner: ${{ github.repository_owner }}
          repositories: "gh-actions,${{ github.repository }}"

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Self-hosted Renovate
        uses: renovatebot/github-action@316d7cd859606d6039a2182b7d69199e9b036835 # v46.2.1
        with:
          renovate-image: ghcr.io/renovatebot/renovate
          renovate-version: 43.180.2
          token: '${{ steps.get_token.outputs.token }}'
        env:
          RENOVATE_REPOSITORIES: "[\"${{ github.repository }}\"]"
          RENOVATE_PACKAGE_RULES: |
            [
              {
                matchManagers: ["github-actions"],
                matchDepNames: ["/msdeleyto\/gh-actions/"],
                enabled: false,
              },
            ]
          RENOVATE_EXTENDS: "[\"config:best-practices\"]"
